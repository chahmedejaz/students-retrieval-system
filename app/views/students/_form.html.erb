<%= form_with(model: student) do |form| %>
  <% if @errors && @errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(@errors.count, "error") %> prohibited this student from being saved:</h2>

      <ul>
        <% @errors.each do |error| %>
          <li><%= error %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :email, style: "display: block" %>
    <%= form.text_field :email %>
  </div>

  <div>
    <%= form.label :course_name, style: "display: block" %>
    <%= form.text_field :course_name, value: @course.course_name %>
  </div>

  <div>
    <%= form.label :credit_hours, style: "display: block" %>
    <%= form.number_field :credit_hours, value: @course.credit_hours %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
